{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Shell": {
    "DisableAuthorization": false
  },
  "Backend": {
    "Url": "",
    "_comment": "Intentionally empty. Aspire injects Backend__Url via env var at orchestration time (resolves to {agent-runtime}/elsa/api). If Studio ever calls a different URL, env var injection broke - do NOT hardcode a fallback here, that hides the bug."
  },
  "Localization": {
    "DefaultCulture": "en-US",
    "SupportedCultures": [ "en-US" ]
  }
}
